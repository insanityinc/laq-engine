syntax = "proto3";

package engine;

import "src/block.proto";

option cc_enable_arenas = true;

message matrix {
  string path = 1;
  int32 nnz = 2;
  int32 nrows = 3;
  int32 ncols = 4;
  map<int32, block> blocks = 5;
}